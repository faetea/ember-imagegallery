<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Create a New Collection</h3>
  </div>
  <div class="panel-body">
    <div class="form-horizontal">
      <div class="form-group">
        <label class="col-xs-12 col-sm-3 col-md-3 control-label">Name</label>
        <div class="col-xs-12 col-sm-6 col-md-7">
          {{input class="form-control" placeholder="name" type="text" value=upload.name}}
        </div>
      </div>

      <div class="form-group">
        <label class="col-xs-12 col-sm-3 col-md-3 control-label">Description</label>
        <div class="col-xs-12 col-sm-6 col-md-7">
          {{input class="form-control" placeholder="description" type="text" value=upload.desc}}
        </div>
      </div>

      <div class="form-group">
        <label class="col-xs-12 col-sm-3 col-md-3 control-label">
          Cover Image
        </label>
        <div class="col-xs-12 col-sm-6 col-md-7">
          {{#file-picker
            accept=".jpg,.jpeg,.gif,.png"
            fileLoaded="fileLoaded"
            readAs="readAsDataURL"}}
            <span class="">Drag here or CLICK to upload a file</span>
          {{/file-picker}}
          {{#if file}}
          <span class="">Click on preview to choose a different Cover.</span>
          {{/if}}
        </div>
      </div>

    </div> {{!-- end form-horizontal --}}
  </div>
  <div class="panel-footer text-right button-space">
    <button class="btn btn-success" {{action 'submit' collection}}>
      Create Collection
    </button>

    <button class="btn btn-default" {{action 'reset' collection}}>
      Cancel
    </button>
  </div>
</div>



{{!-- <form class="form-horizontal">
  <div class="form-group">
    <label class="col-xs-12 col-sm-2 control-label">
      Name
    </label>
    <div class="col-xs-12 col-sm-10">
      {{input class="form-control" id="name" placeholder="Collection Name"
      type="text" value=upload.name}}
    </div>
  </div>

  <div class="form-group">
    <label class="col-xs-12 col-sm-2 control-label">
      Description
    </label>
    <div class="col-xs-12 col-sm-10">
      {{input class="form-control" id="desc" placeholder="Description"
      type="text" value=upload.desc}}
    </div>
  </div>

  <div class="form-group">
    <label class="col-xs-12 col-sm-2 control-label">
      Cover Image
    </label>
    <div class="col-xs-12 col-sm-10">
      {{#file-picker
        accept=".jpg,.jpeg,.gif,.png"
        fileLoaded="fileLoaded"
        readAs="readAsDataURL"}}
        <span class="">Drag here or CLICK to upload a file</span>
      {{/file-picker}}
      {{#if file}}
      <span class="">Click on preview to choose a different Cover.</span>
      {{/if}}
    </div>
  </div>

  <div class="form-group">
    <div class="col-xs-12 col-sm-offset-2 col-sm-10">
      <button {{action "submit"}} class="btn btn-default" type="submit">
        Create Collection
      </button>
    </div>
  </div>
</form> --}}
